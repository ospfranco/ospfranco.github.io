{
  "scripts": {
    "deploy": "./deploy.sh",
    "start": "bundle exec jekyll serve --livereload --drafts --future --port 5000 '$@'"
  },
  "devDependencies": {
    "autoprefixer": "^9.3.1",
    "postcss-cli": "^6.0.1",
    "postcss-import": "^12.0.1",
    "purgecss": "^1.1.0",
    "tailwindcss": "^1.0.0"
  }
}
